import React from 'react';

class Button extends React.Component {
    render() {
        return (
            <button disabled>
                Button
            </button>
        )
    }
}

export default Button;
